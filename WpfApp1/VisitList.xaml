<Page x:Class="WpfApp1.VisitList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VisitList">

    <DockPanel Margin="5">
        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" DockPanel.Dock="Top"/>
        <Button Content="Назад" DockPanel.Dock="Bottom" Width="200" Command="{Binding Back}" Padding="5" Margin="0 5 0 0"/>
        <ListBox DockPanel.Dock="Top" ItemsSource="{Binding Visits}"/>
    </DockPanel>
</Page>
